{
  "runtime": "python3.9",
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn api:app --reload --host 0.0.0.0 --port 3000",
  "installCommand": "pip install -r requirements.txt"
}
